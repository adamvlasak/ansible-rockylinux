version: '3'
services:
  work:
    image: "work:centos"
    restart: unless-stopped
    container_name: work
    hostname: work
    ports:
      - "22:22"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
