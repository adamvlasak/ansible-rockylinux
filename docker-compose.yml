version: '3'
services:
  work:
    image: "work:debian"
    restart: unless-stopped
    container_name: work
    hostname: work
    ports:
      - "22:22"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
