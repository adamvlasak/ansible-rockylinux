version: '3'
services:
  work-machine:
    image: work-machine:latest
    restart: unless-stopped
    container_name: work-machine
    hostname: work-machine
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - /var/run/docker.sock:/var/run/docker.sock
    privileged: yes
