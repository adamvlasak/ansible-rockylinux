# Generated by parse-kickstart
IPADDR="{{ ipv4.address }}"
GATEWAY="{{ ipv4.gateway }}"
NETMASK="{{ ipv4.mask }}"
BOOTPROTO="static"
DEVICE="{{ eth_device }}"
ONBOOT="yes"
{% if ipv6.enabled %}
IPV6INIT="yes"
IPV6ADDR="{{ ipv6.address }}"
IPV6_DEFAULTGW="{{ ipv6.gateway }}"
IPV6_DEFAULTDEV="{{ eth_device }}"
{% else %}
IPV6INIT="no"
{% endif %}
